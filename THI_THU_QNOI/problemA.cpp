#include <bits/stdc++.h>
#include <cstdio>
using namespace std;

#define ll long long

int main() {
  // freopen("PAIRS.INP", "r", stdin);
  // freopen("PAIRS.OUT", "w", stdout);

  ios_base::sync_with_stdio(0);
  cin.tie(nullptr);
  cout.tie(nullptr);

  ll n, cnt = 0;
  cin >> n;
  vector<ll> a(n);
  for (auto &x : a) {
    cin >> x;
  }
  for (auto x : a) {
    if ()
  }
}
