#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(nullptr);

  ll t;
  cin >> t;
  while (t--) {
    ll n, a;
    cin >> n >> a;
    vector<ll> vec(n);
    for (auto &x : vec) {
      cin >> x;
    }
  }
}
