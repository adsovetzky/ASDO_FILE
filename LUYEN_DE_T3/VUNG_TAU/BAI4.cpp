#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(nullptr);
  cout.tie(nullptr);
  long long n, k;
  cin >> n >> k;
  vector<pair<int, int>> a;
  while (n--) {
    int a, b;
    cin >> a >> b;
  }
}
